# 1. 日期与结论

## 1.1 本次简报日期

1. 文档日期：`2026-07-18`。
2. 运行时区：`Asia/Shanghai`。
3. 自动化上次运行时间：`2026-07-17T01:07:18.198Z`。
4. 生成前的 `git status --short --branch` 结果：`## main...origin/main`，仅有未跟踪文件 `docs/morning-brief/2026-07-17.md`。

## 1.2 总体结论

1. **有记录支撑的事实**：当前 `main` 与 `origin/main` 对齐，仓库没有代码层面的分叉。
2. **有记录支撑的事实**：最近一次提交仍是 `d5b524f`，主题为 `chore: update learning docs and add comparison table`。
3. **有记录支撑的事实**：上一份简报记录了 `python -m pytest -q` 通过，结果为 `28 passed, 51 subtests passed in 0.29s`。
4. **有记录支撑的事实**：上一份简报记录了 `python -m py_compile s*_*/code_openai.py` 通过。
5. **根据当前工作区状态推断**：今天没有代码合并阻塞，但真实 `OpenAI` 冒烟验证、`OPENAI_MODEL` 示例值确认，以及学习记录落点仍是主要待办。

# 2. 项目概览

## 2.1 有记录支撑的事实

1. 项目定位来自 `README.md`：这是一个讲解 `coding agent harness` 的学习仓库，核心观点是 `Agent Product = Model + Harness`。
2. 当前主线是根目录 `s01_agent_loop/` 到 `s20_comprehensive/`，它们被标记为新的 `20` 课教程轨道。
3. `docs/learning-plan/05-week-2-engineering-tools.md` 显示第 `2` 周目标是理解 `todo`、`subagent` 和 `skill loading`。
4. `docs/learning-plan/11-acceptance-criteria.md` 显示最终验收要覆盖 `agent harness` 架构图、`s01` 到 `s20` 能力演进表、`mini coding agent`、自定义 `skill` 和总结文档。
5. 最近提交 `d5b524f` 同时修改了 `docs/learning-plan/01` 到 `11` 多个文件，说明当前重点仍在课程文档与示例代码对齐。

## 2.2 根据当前工作区状态推断

1. 项目现在更像是在收敛第 `2` 周的工程工具层，而不是继续扩展新的底层机制。
2. `common/tools.py`、`common/utils.py`、`as_input_item`、`function_call_output` 这类变化说明公共胶水层在持续被抽取和统一。
3. 当前没有发现 `docs/learning-note` 或 `docs/learning-notes` 目录，学习过程记录仍缺少稳定落点。

# 3. 昨日工作

## 3.1 有记录支撑的事实

1. `d5b524f` 更新了 `docs/learning-plan/01-project-positioning.md` 到 `docs/learning-plan/11-acceptance-criteria.md` 的多份文档，并补了比较表。
2. 同一提交还更新了 `common/tools.py` 和 `s02_tool_use/code_openai.py`，说明工具调用示例还在做统一整理。
3. `s05_todo_write/example/demo_pkg/tests/conftest.py` 被新增，用来把 `s05_todo_write/example` 放进测试导入路径。
4. 这次修改闭环了上一份简报里提到的 `demo_pkg` 导入问题，默认 `pytest` 不再需要额外设置 `PYTHONPATH`。
5. `git log --oneline --decorate -8` 没有出现 `d5b524f` 之后的新提交。

## 3.2 根据当前工作区状态推断

1. 昨天的主要工作重心仍是把第 `2` 周学习资料和测试入口整理得更适合新读者直接使用。
2. 相比继续加功能，当前更像是在做文档统一、测试可跑性和教学路径的收尾。

# 4. 未完成工作

## 4.1 有记录支撑的事实

1. 多个示例文件仍默认读取 `OPENAI_MODEL`，且常见默认值是 `gpt-5.5`。
2. 历史简报中还没有看到可复查的真实 `OpenAI` 冒烟验证记录。
3. `docs/learning-plan/05-week-2-engineering-tools.md` 仍要求补齐 `subagent` 隔离说明、`code-review subagent prompt` 草稿、`SkillLoader` 说明、好 `skill` 的 `5` 个标准，以及职责边界表。
4. 当前仓库里没有 `docs/learning-note` 或 `docs/learning-notes` 目录。

## 4.2 根据当前工作区状态推断

1. 真实模型调用仍是外部运行风险，`pytest` 和 `py_compile` 只能证明本地代码和测试入口健康。
2. 如果 `gpt-5.5` 不是当前账号可用模型，新读者按默认配置运行示例仍可能失败。
3. 学习产出如果继续分散在提交记录和晨间简报里，后续回看成本会比较高。

# 5. 今日建议

## 5.1 优先级 `P0`

1. 保持当前测试基线不动，继续以 `python -m pytest -q` 和 `python -m py_compile s*_*/code_openai.py` 作为回归入口。
2. 不要回退 `s05_todo_write/example/demo_pkg/tests/conftest.py`，它已经把默认测试入口修顺了。

## 5.2 优先级 `P1`

1. 确认当前账号可用的 `OPENAI_MODEL`，判断 `gpt-5.5` 是否还能继续作为示例默认值。
2. 配置有效的 `OPENAI_API_KEY` 和 `OPENAI_MODEL` 后，运行 `python s01_agent_loop/code_openai.py` 做一次真实冒烟验证。
3. 冒烟验证时记录 `function_call`、本地 `bash` 输出和 `function_call_output` 是否完整闭环。

## 5.3 优先级 `P2`

1. 先决定学习记录落点，用 `docs/learning-notes/` 还是另一个目录承接第 `2` 周产出。
2. 如果继续用文档形式沉淀，优先补一份第 `2` 周总结，覆盖 `todo`、`subagent`、`skill` 和职责边界表。

# 6. 需要用户处理的事项

1. **【需要你处理】** 事项：确认 `OPENAI_MODEL` 示例值是否继续使用 `gpt-5.5`。原因：多个示例默认值仍是这个模型名，若当前账号不可用会直接影响新读者运行。建议动作：选定一个当前可用模型，并同步更新 `.env.example`、`README.md`、`README-zh.md` 和相关章节说明。截止/优先级：`P1`，建议本周内确认。
2. **【需要你处理】** 事项：执行真实 `OpenAI` 冒烟验证。原因：当前只有本地测试和语法检查记录，还没有带凭据的 `Responses API` 闭环验证。建议动作：配置 `OPENAI_API_KEY` 和有效 `OPENAI_MODEL` 后运行 `python s01_agent_loop/code_openai.py`，并记录是否得到 `OPENAI_SMOKE_OK`。截止/优先级：`P1`，在继续推广示例之前完成。
3. **【需要你处理】** 事项：确认学习产出目录。原因：当前仓库没有 `docs/learning-note` 或 `docs/learning-notes`，第 `2` 周需要一个稳定落点。建议动作：指定目录并补写第 `2` 周总结文档。截止/优先级：`P2`，建议本周内定稿。

# 7. 结论与后续跟进

## 7.1 今日结论

1. **有记录支撑的事实**：当前仓库没有代码冲突，`main` 与 `origin/main` 对齐。
2. **有记录支撑的事实**：`d5b524f` 已经把第 `2` 周相关文档和 `s05` 测试导入问题往前推进了一步。
3. **有记录支撑的事实**：当前可复查的最新验证记录仍是上一份简报中的 `python -m pytest -q` 通过，以及 `python -m py_compile s*_*/code_openai.py` 通过。
4. **根据当前工作区状态推断**：今天的主要风险不在代码本身，而在真实模型可用性和学习记录是否继续落地。
5. **用户决策判断**：今天仍然有需要你处理的事项，重点是 `OPENAI_MODEL`、真实冒烟验证和学习产出目录。

## 7.2 下次自动化应重点检查

1. `python -m pytest -q` 是否继续保持通过。
2. 是否出现真实 `OpenAI` 冒烟验证记录，尤其是 `OPENAI_SMOKE_OK`。
3. `OPENAI_MODEL=gpt-5.5` 是否被确认保留或替换。
4. 第 `2` 周学习产出是否已经落到固定目录。
